import { Outlet } from "react-router-dom";

const GoogleOAuth = () => {

    return (
        <main>
            <h1>Google O-Auth</h1>
            <Outlet />
        </main>
    );
};

export default GoogleOAuth;